'\" t
.\" Automatically generated by Pandoc 3.10.1
.\"
.TH "RAX" "3" "" "Maximum Tension" "CMT Programmer\(aqs Manual"
.SH RAX
.RS
.SS /!\(rs Important
.SS File location: [\f[B]\c
.UR https://github.com/TeomanDeniz/CMT/blob/main/ASM/C/INTEL/RAX.H
📜 CMT/ASM/C/INTEL/RAX.H
.UE \c
\f[R]]
.SS How to include:
Recommended (via master header):
.IP
.EX
#define INCL_CMT_RAX
#include \f[B]\(dqCMT/CMT.H\(dq\f[R]
.EE
.PP
Direct include:
.IP
.EX
#include \f[B]\(dqCMT/ASM/C/INTEL/RAX.H\(dq\f[R]
.EE
.RE
.SS Abstract
With these functions, you\(aqre able to move and get values from the
Intel CPU register RAX and stack with different data sizes and archs.
.PP
Using \f[CR]GET_RAX\f[R] or \f[CR]SET_RAX\f[R] on a non\-Intel CPU will
cause an error on compiling time.
.RS
.PP
/!\(rs Note
.PP
On Intel 32\-BIT, EAX register is used.
On Intel 16\-BIT, AX register is used.
.RE
.SS Contents
.PP
.TS
tab(@);
l.
T{
Contents List
T}
_
T{
\f[CR]#define GET_RAX(VARIABLE)\f[R]
T}
T{
\f[CR]#define get_rax(variable)\f[R]
T}
T{
\f[CR]#define SET_RAX(VALUE)\f[R]
T}
T{
\f[CR]#define set_rax(VALUE)\f[R]
T}
.TE
.PP
   *   *   *   *   *
.SS GET_RAX
.IP
.EX
#define GET_RAX(VARIABLE)
#define get_rax(variable)
.EE
.PP
Sets the value of \f[CR]RAX\f[R] register holding to your variable.
.PP
Example:
.IP
.EX
long long test = 0;

volatile_function_that_returns_42();

get_rax(test);

printf(\(dq%d\(rsn\(dq, test); \f[I]// 42\f[R]
.EE
.PP
   *   *   *   *   *
.SS SET_RAX
.IP
.EX
#define SET_RAX(VALUE)
#define set_rax(value)
.EE
.PP
Sets the \f[CR]RAX\f[R] register\(aqs value to given value or variable.
.PP
\f[B]Examples\f[R]:
.IP
.EX
set_rax(42);
.EE
.IP
.EX
long long value = 42;

set_rax(value);
.EE
.SH AUTHORS
Teoman Deniz.
